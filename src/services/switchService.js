class switchMode {
    async storeContrat() {
// recuperer le type (local ou  en ligne) 
// si le mode est hors ligne alors j'appelle la function de offlineService.storeContrat()
//Si le mode est en ligne
//try catch
    }
}